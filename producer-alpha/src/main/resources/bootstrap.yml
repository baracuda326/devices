server:
  port: 8091
spring:
  application:
    name: producer-alpha
eureka:
  instance:
    prefer-ip-address: on
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
        fetchRegistry: true